#include "WindowPlane.h"

/*
void WindowPlane::Draw(const glm::mat4& parentTransform)
{
    vertexArray.Get()->Bind();
    glDrawElements(GL_TRIANGLES, indexBuffer.Get()->GetCount(), GL_UNSIGNED_INT, nullptr);
    vertexArray.Get()->Unbind();
}
*/
